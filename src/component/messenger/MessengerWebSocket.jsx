import { useState } from "react";

function MessengerWebSocket({ chatroomList }) {
    console.log('웹소켓 페이지에서 chatroomList', chatroomList);

    const [websocket, setWebsocket] = useState(null);

    return (
        <>
        </>)
}
export default MessengerWebSocket;